import 'package:flutter/material.dart';
import '../utils/logger_widget.dart';

class LogsScreen extends StatelessWidget {
  const LogsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const LoggerWidget();
  }
} 